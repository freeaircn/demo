登录流程：前端 & 后端

1 【前端】登录表单submit
  2019-03-12
  TODO: 学习VueX
  handleLogin() {
      this.$refs.loginForm.validate(valid => {
        if (valid) {
          this.loading = true
          this.$store.dispatch('Login', this.loginForm).then(() => {
            this.loading = false
            this.$router.push({ path: this.redirect || '/' })

  2019-03-16
  初步了解vuex和Promise

2 【后端】去掉session，增加Token
  2019-03-13
  TODO:
  1 使用第三方Token包
  2 [issue] 数据库表尝试次数，残留非注册用户尝试登录记录，缺少clear处理
  2019-03-16
  1 已引入JWT包
  3 [issue] 用户未激活，用户名和密码正确登录，没有increase login尝试次数
    修改方法：login方法，密码验证通过，识别用户未激活，return 前，increase login尝试次数

3 【前端】请求携带token
  2019-03-16
  token放置于前端请求哪个字段：axios.interceptors.request请求拦截器中，当本地存有token时，将token放在request header的X-Token字段。

4 【前端】token校验失败后处理
  2019-03-16
  TODO: 前端收到token校验失败，怎么处理
